{
  "name": "cv-generator",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "prepare:pdf-themes": "node scripts/apply-pdf-first-themes.mjs",
    "dev": "npm run prepare:pdf-themes && vite",
    "build": "npm run prepare:pdf-themes && vite build",
    "preview": "vite preview",
    "check": "npm run prepare:pdf-themes && svelte-check --tsconfig ./tsconfig.app.json && tsc -p tsconfig.node.json"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.0.0",
    "@tsconfig/svelte": "^5.0.8",
    "@types/node": "^24.12.2",
    "svelte": "^5.55.4",
    "svelte-check": "^4.4.6",
    "typescript": "~6.0.2",
    "vite": "^8.0.10"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  }
}
